#pragma once

namespace appstyle {
    inline constexpr float SCALE = 1.0f;

    
    inline constexpr unsigned TITLE = static_cast<unsigned>(34 * SCALE);
    inline constexpr unsigned SUBTITLE = static_cast<unsigned>(26 * SCALE);
    inline constexpr unsigned PANEL_TITLE = static_cast<unsigned>(28 * SCALE);
    inline constexpr unsigned PANEL_TEXT = static_cast<unsigned>(26 * SCALE);
    inline constexpr unsigned VECTOR_NODE_VALUE = static_cast<unsigned>(30 * SCALE);
    inline constexpr unsigned VECTOR_NODE_INDEX = static_cast<unsigned>(20 * SCALE);
    inline constexpr unsigned LIST_NODE_VALUE = static_cast<unsigned>(30 * SCALE);
    inline constexpr unsigned LIST_NODE_NULL = static_cast<unsigned>(22 * SCALE);
    inline constexpr unsigned HELP_HEADER = static_cast<unsigned>(30 * SCALE);
    inline constexpr unsigned HELP_LINE = static_cast<unsigned>(24 * SCALE);
    inline constexpr unsigned HUD_SMALL = 18;
    inline constexpr unsigned HUD_MEDIUM = 22;
    inline constexpr unsigned HUD_LARGE = 28;
    inline constexpr unsigned OVERLAY_SMALL = 18;
}
